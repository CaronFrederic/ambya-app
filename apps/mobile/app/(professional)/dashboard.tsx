import { Text } from "react-native";
import { Screen } from "../../src/components/Screen";

export default function Dashboard() {
  return (
    <Screen>
      <Text style={{ fontSize: 22, fontWeight: "700" }}>Pro • Dashboard</Text>
    </Screen>
  );
}
