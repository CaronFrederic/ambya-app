{
  "name": "ambya",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format",
    "test:backend": "pnpm --filter api exec jest --runInBand",
    "check:backend": "pnpm --filter api build && pnpm --filter api exec jest --runInBand",
    "check:mobile": "pnpm exec tsc -p apps\\mobile\\tsconfig.json --noEmit",
    "check:quick": "pnpm --filter api build && pnpm exec tsc -p apps\\mobile\\tsconfig.json --noEmit",
    "check:full": "pnpm check:backend && pnpm check:mobile",
    "check:beta": "pnpm check:full"
  },
  "keywords": [],
  "author": "LAN Consulting",
  "license": "ISC",
  "packageManager": "pnpm@10.29.3"
}
